[package]
name = "pdf_renderer"
version = "0.1.0"
edition = "2021"

[lib]
name = "pdf_renderer"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.24", features = ["extension-module"] }
pdf_oxide = { version = "0.3", features = ["rendering"] }
